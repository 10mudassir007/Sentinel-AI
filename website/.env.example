# Copy this file to .env and adjust the values for your environment

# Backend API base URL
VITE_API_URL=http://localhost:8754

# CNIC used by the Demo page to auto-login to /login and obtain a bearer token.
# Must be a CNIC registered on the backend (used for demo authentication only).
VITE_DEMO_CNIC=
